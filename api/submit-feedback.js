module.exports = async (req, res) => { try { const { feedback, name, email } = req.body; if (!feedback || !feedback.trim()) { return res.status(400).json({ error: "Feedback message is required" }); } console.log(`Received feedback from ${name || "Anonymous"}: ${feedback}`); return res.status(200).json({ message: "Feedback saved successfully", local: true }); } catch (error) { console.error("Server Error:", error); return res.status(500).json({ error: "Server error: " + error.message, local: false }); } };
